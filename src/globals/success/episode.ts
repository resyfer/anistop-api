import { success } from "@success/success";

const episodeCreated = success("Episode created successfully.");
const episodeDeleted = success("Episode deleted successfully.");

export { episodeCreated, episodeDeleted };
