import { success } from "@success/success";

const animeCreated = success("Anime created successfully.");
const animeUpdated = success("Anime updated successfully.");

export { animeCreated, animeUpdated };
