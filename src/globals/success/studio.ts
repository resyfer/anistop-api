import { success } from "@success/success";

const studioAdded = success("Studio added successfully.");
const studioLogoUpdated = success("Studio logo updated successfully.");

export { studioAdded, studioLogoUpdated };
